/**
 * Shared error copy used across the parent portal so the wording stays
 * consistent in API responses and component states.
 */
/** Parent portal: no `parents.email` in the academy DB matches this address (after normalization). */
export const NO_RECORD_ERROR =
  "No parent profile in our academy roster uses this email. Use the same email as on your child's registration, or ask the office to update your parent contact so it matches your portal login.";
