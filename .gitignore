node_modules/
.next/
out/
.turbo/
node_modules/.cache/
.env
.env*.local
npm-debug.log*
.DS_Store

# Player uploads (keep folder via public/uploads/players/.gitkeep)
public/uploads/players/*
!public/uploads/players/.gitkeep

# CMS uploads
public/uploads/cms/*
!public/uploads/cms/.gitkeep

# Persisted CMS snapshot (local/dev — survives server restarts; commit if you want to share defaults)
data/site-content.json
